box:
  id: node:latest
build:
  steps:
    - npm-install
    - npm-test
  after-steps:
    - install-packages:
        packages: ruby
    - wantedly/pretty-slack-notify:
        webhook_url: $SLACK_WEBHOOK_URL